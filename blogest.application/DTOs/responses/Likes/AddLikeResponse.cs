namespace blogest.application.DTOs.responses.Likes
{
    /// <summary>
    /// Response for adding a like to a post.
    /// </summary>
    public record AddLikeResponse(string message,bool success);
}
